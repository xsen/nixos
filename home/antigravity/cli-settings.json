{
  "allowNonWorkspaceAccess": false,
  "enableTelemetry": false,
  "notifications": true,
  "editor": "zed --wait",
  "permissions": {
    "allow": [
      "command(cd)",
      "command(ls)",
      "command(cat)",
      "command(head)",
      "command(tail)",
      "command(find)",
      "command(grep)",
      "command(rg)",
      "command(jq)",
      "command(wc)",
      "command(file)",
      "command(which)",
      "command(strings)",
      "command(stat)",
      "command(echo)",
      "command(mkdir)",
      "command(touch)",
      "command(ghostty)",
      "command(hyprctl)",
      "command(git)",
      "command(nix)",
      "command(nixfmt)",
      "command(nix-instantiate)",
      "command(nix-store)",
      "command(nix-shell)",
      "command(nix-build)",
      "command(php artisan test)",
      "command(composer)",
      "command(phpunit)",
      "command(cargo check)",
      "command(cargo test)",
      "command(cargo clippy)",
      "command(cargo fmt)",
      "command(npm test)",
      "command(npm run test)",
      "command(npm run lint)",
      "command(npm run format)",
      "command(yarn test)",
      "command(pnpm test)",
      "command(bun test)",
      "command(deno test)",
      "command(pytest)",
      "command(eslint)",
      "command(prettier)",
      "read_file(/home/evgeny/.gemini/config/skills/**)",
      "read_file(/home/evgeny/.gemini/antigravity-cli/**)"
    ],
    "ask": [
      "command(git push)",
      "command(git reset --hard)",
      "command(git clean)",
      "command(rm)",
      "command(sudo)",
      "command(nh-all)",
      "command(nh-os)",
      "command(nh-home)"
    ]
  },
  "runningLightSpeed": "fast",
  "showFeedbackSurvey": false,
  "statusLine": {
    "type": "command",
    "command": "/home/evgeny/.gemini/antigravity-cli/statusline-command.sh",
    "enabled": true
  },
  "trustedWorkspaces": [
    "/home/evgeny/Code/Soft/eve-zavod",
    "/home/evgeny/Code/nixos/xsen"
  ]
}
